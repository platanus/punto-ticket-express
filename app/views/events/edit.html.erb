<div ng-controller="FormEventCtrl" ng-init='init(<%= raw @event.to_json(:include => :ticket_types) %>, <%= raw current_user.producers.to_json %>, <%= @event.is_past_event? %>)'>
  <%= render partial: 'edit_top_navbar', locals: {title: t('.title'), show_buttons: [:preview, :publish]} %>

  <div class="row" id="page_event" style="margin-top: 30px;">
  	<div class="span3">
  		<%= render 'sidebar' %>
  	</div>
  	<div class="span9">
  		<%= render 'form' %>
  	</div>
  </div>
</div>
